export interface CardOwnedDTO {
    cardId: number,
    cardName: string,
    cardImageURL: string,
    cardOwnedId: number,
    cardPrice: number,
    editionName: string,
    editionCode: string,
    condition: string,
    quantity: number
}